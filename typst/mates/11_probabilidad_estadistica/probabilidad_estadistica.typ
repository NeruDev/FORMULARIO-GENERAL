// =====================================================================
// 11 · Probabilidad y Estadística — Formulario General (Typst)
//
// Estado: PENDIENTE DE CONTENIDO
//
// Este módulo está contemplado en `ARCHITECTURE.md` §2.1.11, pero NO
// existe ningún compendio de Probabilidad y Estadística en `latex/`:
// ni `formulario_mates.tex` ni ninguno de los tres formularios por nivel
// cubren esta materia.
//
// Se crea como marcador documentado para preservar la estructura modular
// acordada, en lugar de inventar contenido fuera del alcance de la
// migración LaTeX -> Typst. Ver `typst_plan.md` §8 (riesgos).
//
// Los temas previstos, según ARCHITECTURE.md §2.1.11, son:
//   1. Estadística descriptiva:
//      media, mediana, moda, varianza, desviación estándar.
//   2. Combinatoria:
//      permutaciones y combinaciones.
//   3. Probabilidad:
//      probabilidad condicional y regla de Bayes.
//   4. Distribuciones discretas:
//      Binomial y Poisson.
//   5. Distribuciones continuas:
//      Normal (gaussiana).
//
// Cuando se redacte el contenido, debe seguir el contrato de estilo de
// `typst/lib/CONVENCIONES.md` y usar la misma estructura que el resto de
// módulos: `#nivel(...)`, `#tema(...)`, metadatos `///` y bloques `$ ... $`.
// =====================================================================

#import "../../lib/formulas.typ": nivel, tema

= Probabilidad y Estadística

#nivel("Nivel Intermedio")

#tema("11.0 Módulo pendiente de contenido")

Este módulo está reservado para las fórmulas de *Probabilidad y Estadística*
(estadística descriptiva, combinatoria, probabilidad condicional, regla de
Bayes y distribuciones Binomial, Poisson y Normal).

No hay compendio de origen en `latex/`, por lo que el contenido se añadirá
en una sesión posterior a partir de `ARCHITECTURE.md` §2.1.11.
